services:
  db:
    image: mysql:8
    container_name: taskmananger-spring
    restart: always
    ports:
      - "3309:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=taskmananger
    volumes:
      - data_mysql:/var/lib/mysql

volumes:
  data_mysql: