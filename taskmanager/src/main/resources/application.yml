spring:
  datasource:
    url: jdbc:mysql://localhost:3309/taskmananger?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true